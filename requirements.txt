# Web framework / pipeline host
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database (async)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0          # local-dev / test SQLite driver
alembic==1.14.1

# Config
pydantic==2.12.5
pydantic-settings==2.7.0
python-dotenv==1.0.1

# AI — single agent via LiteLLM (Claude by default)
litellm==1.57.3

# Scheduler (in-process; no Redis/worker in v1)
apscheduler==3.11.0

# HTTP client (Telegram API + publishers)
httpx==0.27.2

# Linting / type checking / testing (dev)
ruff==0.9.1
mypy==1.14.1
pytest==8.3.4
pytest-asyncio==0.24.0
